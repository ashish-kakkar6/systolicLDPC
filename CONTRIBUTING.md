# Contributing

Please open an issue before substantial changes so the scope can be discussed first.
Make sure the relevant example flow still works and that `make test` and `make docs` pass.
Keep new examples in the same `build.py -> run.py -> read.py` structure as the existing public flows.
Match the style of the surrounding RTL, example, and documentation files.
